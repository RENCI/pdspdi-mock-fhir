version: '3'
services:
  pdspi-fhir-example-test:
    build:
      context: .
      dockerfile: test/Dockerfile
    command: ["-s", "-x", "-v"] # , "-k", "test_post_bundle_medication_request"]
    depends_on:
      - pdspi-fhir-example
